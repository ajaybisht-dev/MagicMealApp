import { StyleSheet } from "react-native";

const DefaultStyle = StyleSheet.create({
  imageSize: {
    height: "100%",
    width: "100%",
    resizeMode : "contain"
  },
  textStyle: {
    
  }
});

export { DefaultStyle };